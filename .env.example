# Environment Variables Configuration

# ============================================
# MongoDB Configuration
# ============================================
# Docker: Use service name 'mongo'
# Local: Use 'localhost'
MONGO_URI=mongodb://mongo:27017/r4b_db

# ============================================
# JWT Secret
# ============================================
# Generate using: openssl rand -hex 32
# CRITICAL: Generate a unique secret for each environment!
# Example command: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_MUST_BE_32_BYTES_OR_MORE

# ============================================
# Database Field Encryption Key (NEW)
# ============================================
# Used to encrypt sensitive fields in Settings collection
# CRITICAL: Generate unique 32-byte key, different from JWT_SECRET
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_encryption_key_32_bytes_hex

# ============================================
# Supabase Configuration (NEW)
# ============================================
SUPABASE_URL=your-supabase-project-url
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# ============================================
# Email Configuration (Gmail SMTP)
# ============================================
# Use Gmail App Password, not your actual password
# Guide: https://support.google.com/accounts/answer/185833
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password_16_chars

# ============================================
# Admin Email (for notifications)
# ============================================
# Email address to receive admin notifications (tickets, new orders, etc.)
ADMIN_EMAIL=admin@yourdomain.com

# ============================================
# Client URL
# ============================================
# URL where frontend is hosted
# Docker: http://localhost:8080 (nginx port)
# Local dev: http://localhost:5173 (vite dev server)
CLIENT_URL=http://localhost:8080
